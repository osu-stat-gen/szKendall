% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/WSS_percentage.R
\name{WSS_percentage}
\alias{WSS_percentage}
\title{Calculate the within-cluster sum of squares percentage (WSS\%)}
\usage{
WSS_percentage(clust_result, distance_matrix)
}
\arguments{
\item{clust_result}{A vector of length \code{n_single}, with each element indicating the index of cluster where the cell belongs.}

\item{distance_matrix}{An \code{n_single}x\code{n_single} symmetric dissimilarity matrix that \code{cluster_result} is based on.}
}
\value{
A numeric value of WSS\%, between 0 and 1.
}
\description{
This function computes the within-cluster sum of squares percentage (WSS\%), given the clustering labels (from any clustering algorithm) and the distance matrix on which the clustering was performed.
}
\examples{
set.seed(1234)
km1 <- kmeans(t(scale(sim1.data)), centers=3, nstart=10, iter.max=100, algorithm="Hartigan-Wong")
euclid.diss <- as.matrix(dist( t(scale(sim1.data)) ))
WSS_percentage(km1$cluster, euclid.diss)
}
