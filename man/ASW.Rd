% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ASW.R
\name{ASW}
\alias{ASW}
\title{Calculate the average silhouette width (ASW)}
\usage{
ASW(clust_result, distance_matrix)
}
\arguments{
\item{clust_result}{A vector of length \code{n_single}, with each element indicating the index of cluster where the cell belongs.}

\item{distance_matrix}{An \code{n_single}x\code{n_single} symmetric dissimilarity matrix that \code{cluster_result} is based on.}
}
\value{
A numeric value of ASW, between -1 and 1.
}
\description{
This function computes the average silhouette width (ASW), given the clustering labels and the distance matrix on which the clustering was performed.
}
\examples{
set.seed(1234)
km1 <- kmeans(t(scale(sim1.data)), centers=3, nstart=10, iter.max=100, algorithm="Hartigan-Wong")
euclid.diss <- as.matrix(dist( t(scale(sim1.data)) ))
ASW(km1$cluster, euclid.diss)
}
