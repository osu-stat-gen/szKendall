% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MIS.R
\name{MIS}
\alias{MIS}
\title{Calculate the minimum isolation score (MIS)}
\usage{
MIS(clust_result, distance_matrix)
}
\arguments{
\item{clust_result}{A vector of length \code{n_single}, with each element indicating the index of cluster where the cell belongs.}

\item{distance_matrix}{An \code{n_single}x\code{n_single} symmetric dissimilarity matrix that \code{cluster_result} is based on.}
}
\value{
A numeric value of MIS across all clusters, excluding the clusters that contain only a single cell (as their isolation score is infinity).
}
\description{
This function computes the minimum isolation score (MIS), given the clustering labels and the distance matrix on which the clustering was performed.
}
\examples{
set.seed(1234)
km1 <- kmeans(t(scale(sim1.data)), centers=3, nstart=10, iter.max=100, algorithm="Hartigan-Wong")
euclid.diss <- as.matrix(dist( t(scale(sim1.data)) ))
MIS(km1$cluster, euclid.diss)
}
