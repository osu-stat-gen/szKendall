% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ARI.R
\name{ARI}
\alias{ARI}
\title{Calculate the adjusted Rand index (ARI)}
\usage{
ARI(true_label, clust_result)
}
\arguments{
\item{true_label}{A vector of length \code{n_single}, with each element indicating the true class (cell type or subtype) of each observation.}

\item{clust_result}{A vector of length \code{n_single}, with each element indicating the index of the cluster where the corresponding observation is assigned.}
}
\value{
A numeric value of the Adjusted Rand Index (ARI), bounded above by 1
}
\description{
This function computes the Adjusted Rand Index (ARI) between two clustering label vectors.
}
\examples{
set.seed(1234)
n_single <- 90
true_label <- factor(rep(1:3, each = n_single/3))
km1 <- kmeans(matrix(rnorm(n_single*10), nrow = n_single), centers = 3,
              nstart = 10, iter.max = 100, algorithm = "Hartigan-Wong")
ARI(true_label, km1$cluster)
}
