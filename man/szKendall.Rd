% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/szKendall_main.R, R/zzz.R
\docType{package}
\name{szKendall}
\alias{szKendall-package}
\alias{szKendall}
\title{Calculate szKendall-based dissimilarity among a set of single cells using scHi-C data.}
\usage{
szKendall(counts, dist.method = "szKendall")
}
\arguments{
\item{dist.method}{Character string specifying which szKendall dissimilarity measure to use.
Supported options: \code{"szKendall"} (default), \code{"szKendall1"}. See @detail below.}

\item{contacts}{Single-cell Hi-C input data.This package supports three input formats:
\itemize{
\item \strong{(1)} a list of scHi-C 2D matrices of size \deqn{n} (e.g. the number of bins in a chromosome);
\item \strong{(2)} a 3D array of dimension \deqn{n \times n \times \#cells};
\item \strong{(3)} a \deqn{\#locus\text{-}pairs \times \#cells} matrix.
}}
}
\value{
A list of three squared symmetric dissimilarity matrices will be returned, one for szKendall(1),
one for Euclidean, and one for Kendall (the last two was for the comparison purpose when drawing the heatmaps).
}
\description{
This function takes scHi-C data input and computes dissimilarity between each pair of single cells.
In addition to the selected szKendall method, dissimilarity based on Euclidean and Kendall will also be provided for comparison.

This package includes the functions that calculate the szKendall dissimilarity (and its two variants, szKendall1 and szKendall2) between two vectorized single-cell Hi-C data, given their structural zero positions (either "ground truth" for simulated data or inferred structural zeros for real data).
}
\details{
This function first calls \code{bind_to_matrix()} to prepare the input data into the \deqn{\#locus\text{-}pairs \times \#cells} matrix format if necessary.
Then it calls the function corresponding to the \code{dist.method} specified.

When \code{dist.method = "szKendall"}, pairwise contributions are weighted
according to the genomic distance difference using
\deqn{
  W^{K}_{\,|\!|j-i|\!|-|\!|v-u|\!|} =
  \left( n - 1 - \bigl|\,|j-i| - |v-u|\,\bigr| \right)^{-0.4}
}
where \eqn{i,j} index loci of one contact pair and \eqn{u,v} index loci of the
other contact pair.

For \code{dist.method = "szKendall1"},
\deqn{
  W^{K1}_{\,|\!|j-i|\!|-|\!|v-u|\!|} = 1
}
}
\section{Warning}{

\itemize{
\item Register a parallel backend using the following lines if it is not done first. Other wise this function uses \code{NumCores() - 1} cores by default.
}
}

\examples{
foreach::registerDoSEQ()


}
\seealso{
\code{\link{bind_to_matrix}},
\code{\link{szKendall.diss}},
\code{\link{szKendall.diss1}}
}
\author{
\strong{Maintainer}: Yongqi Liu \email{yqliu0123@gmail.com}

}
\keyword{internal}
